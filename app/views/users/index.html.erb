<main>
        <!--<p id="notice">Welcome! You have signed up successfully.</p>-->
<div class="container px-5 px-sm-0">
<%= render 'users/user', users: @users, newbook: @newbook %>
　
  
</div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class="table table-hover table-inverse">
    <thead>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </thead>
  <tbody>
    <% @user.each do |user| %>
    <tr>
    <td><%=link_to image_tag user.get_profile_image %>
      </td>
      
      <td><%=user.name %></td>
      <td><div class="btn btn-sm btn-success">
        <%=link_to "Show", user_path(user) %>
      </div></td>
      <% end %>
      <th colspan="3"></th>
      </tr>
  </tbody>
  </table>
    </div>
  </div>
</div>

      </main>
  <footer>
  <div class="container mt-5">
  	<div class="row">
    	<div class="mx-auto">
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>
  </footer>