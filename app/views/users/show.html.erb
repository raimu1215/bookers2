<main>
<div>
  <%= flash[:notice] %>
</div>
<div class="container px-5 px-sm-0">

 <%= render 'users/user', users: @user, newbook: @newbook %>
　

    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books detail</h2>
      <table class="table table-hover table-inverse">
     <thead>
      <tr>
      <th></th>
      <th>title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
      </tr>
     </thead>
     <tbody>
      <tr>
      <% @books.each do |book| %>
      <td><%= link_to user_path(book.user.id) do %>
          <%= image_tag book.user.get_profile_image %>
          <% end %></td>
      <th><%=link_to book.title, book_path(book)%></th>
      <th><%= book.body %></th>
      <td colspan="3"></td>
      </tr>
     </tbody>
     <% end %>
</table>
    </div>
  </div>
</div>

      </main>
    <footer class="footer">
  <div class="container mt-5">
  	<div class="row">
    	<div class="mx-auto">
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>
</footer>

  

</body></html>